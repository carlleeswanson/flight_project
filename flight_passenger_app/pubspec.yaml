name: flight_passenger_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flight_translations_library:
    path: ../flight_translations_library

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - packages/flight_translations_library/lang/en.json
    - packages/flight_translations_library/lang/es.json
    - packages/flight_translations_library/lang/fr.json
